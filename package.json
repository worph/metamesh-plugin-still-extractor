{
  "name": "@metazla/plugin-still-extractor",
  "version": "1.0.0",
  "packageManager": "pnpm@10.28.0",
  "description": "MetaMesh plugin: Extracts a representative frame from a video file and stores it as the record's `still`",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
